# Cheat Sheet: Tutti i Comandi di Kubernetes e Docker
In questo capitolo finale, raccogliamo tutti i comandi essenziali che abbiamo incontrato durante il percorso "Fondamentali Kube". Questa guida è pensata per essere un riferimento rapido e leggibile.
## 🐳 1. Fondamentali Docker
Gestione del ciclo di vita di container e immagini.
| Comando | Descrizione |
|:---|:---|
| `docker build -t name:tag .` | Crea un'immagine dal Dockerfile corrente. |
| `docker run -d -p 80:80 name` | Avvia un container in background mappando le porte. |
| `docker ps [-a]` | Elenca i container in esecuzione (o tutti). |
| `docker logs -f <id>` | Visualizza i log in tempo reale. |
| `docker exec -it <id> sh` | Entra in un container in esecuzione. |
| `docker images` | Elenca le immagini scaricate localmente. |
| `docker rm <id>` | Rimuove un container. |
| `docker rmi <id>` | Rimuove un'immagine. |
| `docker system prune` | Pulisce tutto ciò che non è utilizzato. |
## 🏗️ 2. Architettura e Esplorazione
Comandi per l'ispezione del cluster.
| Comando | Descrizione |
|:---|:---|
| `kubectl cluster-info` | Mostra gli endpoint dei servizi del Control Plane. |
| `kubectl get nodes` | Elenca i nodi del cluster. |
| `kubectl describe node <name>` | Dettagli tecnici e risorse di un nodo. |
| `kubectl version --short` | Mostra le versioni di K8s. |
| `kubectl api-resources` | Elenca tutte le risorse supportate dal cluster. |
## 📦 3. Gestione Risorse
Operazioni su Pod, Deployment e Service.
| Comando | Descrizione |
|:---|:---|
| `kubectl apply -f file.yaml` | Crea/Aggiorna risorse in modo dichiarativo. |
| `kubectl get pods\|deploy\|svc` | Elenca le risorse specifiche. |
| `kubectl describe <res> <name>` | Troubleshooting e dettagli completi. |
| `kubectl delete -f file.yaml` | Rimuove le risorse. |
| `kubectl scale deploy <name> --replicas=3` | Scala il numero di pod. |
| `kubectl edit deploy <name>` | Modifica la configurazione "al volo". |
## 🌐 4. Networking e Debug
Comandi per risolvere problemi di connettività.
| Comando | Descrizione |
|:---|:---|
| `kubectl get ingress\|netpol` | Elenca oggetti di rete. |
| `kubectl get endpoints` | Mostra gli IP reali dietro i servizi. |
| `kubectl port-forward pod/<name> 8080:80` | Proxy locale verso un pod. |
| `kubectl logs [-f] <pod-name>` | Analisi dei log del Pod. |
| `kubectl exec -it <pod-name> -- sh` | Shell interattiva nel Pod. |
## 💾 5. Storage
Persistenza dei dati.
| Comando | Descrizione |
|:---|:---|
| `kubectl get pv\|pvc\|sc` | Gestione volumi e classi di storage. |
## 🔒 6. Sicurezza e RBAC
Permessi e controllo accessi.
| Comando | Descrizione |
|:---|:---|
| `kubectl get sa` | Elenca ServiceAccounts. |
| `kubectl auth can-i <verb> <res>` | Verifica i tuoi permessi. |
| `kubectl get roles\|clusterroles` | Visualizza i ruoli definiti. |
## ⚓ 7. Helm
Package management.
| Comando | Descrizione |
|:---|:---|
| `helm install <name> <chart>` | Installa un'applicazione. |
| `helm list` | Elenca le release. |
| `helm upgrade\|uninstall <name>` | Aggiorna o rimuovi. |
| `helm template <chart>` | Debug dei manifest generati. |
## ⚙️ 8. Operatori e Avanzato
Estensibilità del cluster.
| Comando | Descrizione |
|:---|:---|
| `kubectl get crd` | Elenca le definizioni personalizzate. |
| `kubectl explain <resource>` | Documentazione inline dei campi. |
---
**Prossimo Passo**: Ora che hai ripassato tutti i comandi, sei pronto per l'**Esame Finale**!
